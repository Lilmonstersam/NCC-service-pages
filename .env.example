# No environment variables are currently required to run or build this project.
#
# GEMINI_API_KEY is referenced in vite.config.ts as a leftover from the original
# AI Studio scaffold, but nothing in src/ uses it. Safe to leave unset.
